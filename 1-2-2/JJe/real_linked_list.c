#include <stdio.h>
#include <stdlib.h>

struct NODE{
    int key;
    struct NODE *next;
};

struct list{
    struct NODE *head;
    struct NODE *current;
};

void insert(struct list *list, int data){
    
}

